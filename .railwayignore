# O que NÃO subir no contexto do `railway up` (build é feito na nuvem via Dockerfile).
node_modules
**/node_modules
dist
**/dist
.turbo
**/.turbo
.env
.env.*
!.env.example
.git
**/*.log
coverage
**/coverage
scratchpad

# ⚠️ 89 MB de capturas de tela que o build NUNCA lê — e que faziam o `railway up`
#    subir 115 MB e falhar com 500 no upload. Material de referência do
#    levantamento, não código.
docs/referencias
**/imagens de referencia
